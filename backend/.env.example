NODE_ENV=development
PORT=4000
# Docker PostgreSQL (default):
DATABASE_URL=postgresql://sphere:sphere_dev_password@localhost:5432/sphere_perps

# Neon cloud (no Docker) — get free DB at https://neon.tech:
# Use connect_timeout so cold starts (scale-to-zero) don't fail Prisma:
# DATABASE_URL=postgresql://user:pass@ep-xxx.region.aws.neon.tech/neondb?sslmode=require&connect_timeout=30
JWT_SECRET=dev-jwt-secret-change-in-production
CORS_ORIGIN=http://localhost:5173

# Sphere SDK
SPHERE_ORACLE_API_KEY=sk_ddc3cfcc001e4a28ac3fad7407f99590
SPHERE_WALLET_API_URL=https://wallet-api.unicity.network
SPHERE_TREASURY_NAMETAG=sphere-perps-treasury
SPHERE_TREASURY_MNEMONIC=
SPHERE_DATA_DIR=./sphere-data
SPHERE_DEVICE_ID=sphere-perps-backend

# AI (mock | openai | spacexai)
AI_PROVIDER=mock
AI_API_KEY=

# Admin
ADMIN_WALLET_PUBKEYS=